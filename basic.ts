let username : string = 'daniel'
let login : boolean = true

const user: Array<string> = username.split('')

const arr = [1,2, 'hola']

interface Person {
  first: string
  last: string
}
const person : Person = {
  first: 'daniel',
  last: 'lamba'
}

const ids: Record<number, string> = {
  10: 'a',
  20: 'b',
  30 : 'c'
}

